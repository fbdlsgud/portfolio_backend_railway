# Dev Workflow Rules

- Prefer project-defined commands from `.ai-local/project-profile.yaml`.
- Read task scripts before running unfamiliar commands.
- Do not install dependencies unless needed.
- Report failed commands with exact failure context.
- Keep setup scripts portable and avoid hardcoded user paths.

