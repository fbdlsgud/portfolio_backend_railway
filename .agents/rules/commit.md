# Commit Rules

- Commit only when the user asks.
- Stage only task-owned files.
- Use the project's commit convention when defined.
- Include docs/ai records when they are part of the task.
- Do not commit secrets, generated caches, or unrelated local changes.

