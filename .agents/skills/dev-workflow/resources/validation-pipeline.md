# Validation Pipeline

Common validation order:

1. format/check formatting
2. lint
3. typecheck
4. unit tests
5. integration/e2e tests
6. build
7. browser/device verification

Use the commands defined in `.ai-local/project-profile.yaml` first.

